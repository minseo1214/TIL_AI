-> 리스트
list와 arr는 조금 다르다.
arr는 항상 연속된 주소값을 가지지만 list는 그렇지 않을 수 있다 하지만 파이썬에서 list란 일반적인 arr(배열)과 같은 의미이다.
a=[0,2,1]//이런식으로 사용함.
* 슬라이싱은 [:3] 이런식으로 앞자리를 생략하면 인덱스 0부터인 것이고 [1:] 이렇게 뒷자리를 생략하면 마지막까지를 의미한다.
*[:]이렇게도 사용가능(전부 출력)

->튜플
리스트와 튜플은 매우 유사하지만 튜블은 define list 같은 느낌 -> 수정하지 못한다는 뜻이다.
대괄호를 이용해서 표현했던 리스트와는 다르게 소괄호를 이용함.
a[1,2,3] #리스트
a(1,2,3) #튜플
